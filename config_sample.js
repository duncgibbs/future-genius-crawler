// copy this to config.js and update with desired values
// The future_api_id is the API id for Future. You can change this if you want to bother a different artist.
// But why would you want to do that?
module.exports = {
    client_id: '[GENIUS_API_CLIENT_ID]',
    client_secret: '[GENIUS_API_CLIENT_SECRET]',
    redirect_uri: '[GENIUS_API_CLIENT_REDIRECT_URI]',
    future_api_id: 2197
}
